<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Quên mật khẩu</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="auth-body">
  <div class="auth-container">
    <h2 class="auth-title">QUÊN MẬT KHẨU</h2>

    <!-- Hiển thị lỗi nếu có -->
    <% if (error_message && error_message.length > 0) { %>
      <div class="flash-error"><%= error_message[0] %></div>
    <% } %>
    

    <!-- Hiển thị thông báo lỗi nếu có -->
    <% if (messages.success && messages.success.length > 0) { %>
      <div class="success-message"><%= messages.success[0] %></div>
    <% } %>

    <!-- Form để nhập email khôi phục -->
    <form action="/forgot" method="POST" class="auth-form">
      <div class="form-group">
        <label for="email">Email khôi phục:</label>
        <input type="email" id="email" name="email" required
               oninvalid="this.setCustomValidity('Vui lòng nhập email')"
               oninput="this.setCustomValidity('')" />
      </div>
      <p class="hint-text">Nếu bạn là nhân viên, vui lòng liên hệ chủ cửa hàng để lấy lại mật khẩu!</p>
      <div class="form-actions">
        <button type="submit" class="btn-primary">Lấy lại mật khẩu</button>
        <a href="/login" class="btn-secondary">Quay lại đăng nhập</a>
      </div>
    </form>
  </div>
</body>
</html>
