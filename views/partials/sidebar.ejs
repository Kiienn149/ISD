<div class="sidebar" style="width: 220px; background: #007bff; height: 100vh; color: white;">
  <ul style="list-style: none; padding: 0; margin: 0;">
    <li style="padding: 15px;">
      <a href="/home-partial" class="ajax-link" style="color: white; text-decoration: none;">🏠 Trang chủ</a>
    </li>
    <li style="padding: 15px;">
      <a href="/customer" class="ajax-link" style="color: white; text-decoration: none;">👥 Khách hàng</a>
    </li>

    <% if (user.role === 'owner' || user.role === 'manager') { %>
    <li style="padding: 15px;">
      <a href="/products" class="ajax-link" style="color: white; text-decoration: none;">📦 Sản phẩm</a>
    </li>
    <% } %>

    <% if (user.role === 'owner' || user.role === 'manager' || user.role === 'staff') { %>
    <li style="padding: 15px;">
      <a href="/order" class="ajax-link" style="color: white; text-decoration: none;">📑 Đơn hàng</a>
    </li>
    <% } %>

    <% if (user && user.role === 'owner') { %>
    <li style="padding: 15px;">
      <a href="/account" class="ajax-link" style="color: white; text-decoration: none;">👤 Tài khoản</a>
    </li>
    <li style="padding: 15px;">
      <a href="/settings" class="ajax-link" style="color: white; text-decoration: none;">⚙️ Thiết lập</a>
    </li>
    <% } %>
  </ul>
</div>
